#!/bin/sh
# $Id: gmond.init.solaris,v 1.1 2007/04/28 00:02:57 anoop Exp $
#
# description: gmond startup script
#
. /lib/svc/share/smf_include.sh

PATH=$PATH:/opt/ganglia/sbin
GMETAD=gmond
export PATH

RETVAL=0

case "$1" in
   start)
      echo "Starting GANGLIA gmond"
      [ -f `which $GMETAD` ] || exit 1
      $GMETAD
      RETVAL=$?
      echo
      [ $RETVAL -eq 0 ] && touch /var/lock/gmond
      ;;

  stop)
      echo "Shutting down GANGLIA gmond"
      pkill -TERM $GMETAD
      RETVAL=$?
      echo
      [ $RETVAL -eq 0 ] && rm -f /var/lock/gmond
      ;;

  restart|reload)
      $0 stop
      $0 start
      RETVAL=$?
      ;;
  *)
      echo "Usage: $0 {start|stop|restart}"
      exit 1
esac

exit $RETVAL
